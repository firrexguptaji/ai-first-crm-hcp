import "./header.css";

export default function Header() {
    return (
        <header className="app-header">
            <h1 className="interaction-title">
                Log HCP Interaction
            </h1>
        </header>
    );
}